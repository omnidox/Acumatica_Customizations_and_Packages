Conclusion
The two reviewed folders are not causing the specific repeated PickedForPack/GetSplits() and barcode N+1 problem. That remains attributable to the deployed WMS.PackModeLogicExt.DecorateScanState code reviewed earlier.
Your PackTopRowOnlyScanGuard does add measurable secondary overhead and should be optimized afterward:
First fix WMS.PackModeLogicExt barcode handling to eliminate one full split load and 64 repeated INItemXRef queries.
Then prevent PackTopRowOnlyScanGuard from building large debug strings when tracing is disabled.
Reuse GetExpectedTopRow() results within a scan instead of querying them repeatedly.
Disable tracing in customWMSManualPackTransfer for production.